# dossier
## MCP

Le serveur MCP `21st` (https://21st.dev/api/mcp) est déclaré dans `.mcp.json`.
La clé d'API n'est pas versionnée : exporte-la avant de lancer Claude Code.

```bash
export TWENTY_FIRST_API_KEY="21st_sk_..."
```
